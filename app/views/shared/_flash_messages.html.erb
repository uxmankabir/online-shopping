<% flash.each do |type, message|%>
  <div class="alert alert-<%= type %> alert-dismissible pl-4 fade show" role="alert">
    <%= message %>
    <button type="button" class="close" data-dismiss="alert" aria-label="Close">
      <span aria-hidden="true">&times;</span>
    </button>
  </div>
<% end %>